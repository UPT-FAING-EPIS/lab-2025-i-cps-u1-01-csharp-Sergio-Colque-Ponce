name: Ab
'on':
- push
- repository_dispatch         
jobs:
  set-gh-token:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Exportar GH_TOKEN para CLASSROOM.YML
        run: echo "GH_TOKEN=${{ secrets.GH_TOKEN_V2 }}" >> $GITHUB_ENV
